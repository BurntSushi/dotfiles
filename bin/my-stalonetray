#!/usr/bin/env bash

if [ $# != 1 ]; then
  echo "$(basename "$0") <height>" >&2
  exit 1
fi
height="$1"
shift

stalonetray \
  --background '#ffffff' \
  --geometry 1x1-0-0 \
  --icon-size "$height" \
  --icon-gravity E \
  --grow-gravity E \
  --window-strut bottom \
  "$@"
