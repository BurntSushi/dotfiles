#!/bin/bash

if [ $# = 0 ]; then
  echo "Usage: $(basename "$0") <singleton-media-file> ..." >&2
  exit 1
fi

for f; do
  dir="$(dirname "$f")"
  base="$(basename "$f")"
  name="${base%.*}"
  newdir="$dir/$name"
  mkdir -p "$newdir"
  mv "$f" "$newdir"/
done
