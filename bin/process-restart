#!/bin/sh

process="$1"
shift
if [ -z "$process" ]; then
  echo "Usage: $(basename "$0") <process-name>" >&2
  exit 1
fi
if pgrep "^$process$" &> /dev/null; then
  killall "$process"
fi
exec "$process" "$@"
