#!/bin/sh

process="$1"
shift
if [ -z "$process" ]; then
  echo "Usage: $(basename "$0") <process-name>" >&2
  exit 1
fi
if pgrep -f "(^|/)$process($|\\s+)" &> /dev/null; then
  exit 0
fi
exec "$process" "$@"
